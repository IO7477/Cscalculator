
  # CScalculator

  This is a code bundle for CScalculator. The original project is available at https://www.figma.com/design/hwiHP6rC0KMrsgwxRMQf3H/CScalculator.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  